// Stubbed for Expo Go — AdMob requires a custom dev client.
// Re-enable when building with EAS / custom dev client.
export const INTERSTITIAL_AD_INTERVAL = 5;
export function useInterstitialAd() {
  return { showAd: () => {} };
}
